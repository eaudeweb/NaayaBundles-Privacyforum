from setuptools import setup, find_packages

setup(
    name='NaayaBundles-Privacyforum',
    description="Server-specific bundles, Privacyforum at Enisa",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
